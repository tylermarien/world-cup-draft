<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : t('app.name') %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to t('app.name'), root_path %></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"></a></li>
      </ul>
      <section class="top-bar-section">
        <% if user_signed_in? %>
          <ul class="left">
            <li><%= link_to t('nav.teams'), teams_path %></li>
            <li><%= link_to t('nav.countries'), countries_path %></li>
            <li><%= link_to t('nav.players'), players_path %></li>
            <li><%= link_to t('nav.matches'), matches_path %></li>
            <li><%= link_to t('nav.goals'), goals_path %></li>
          </ul>
        <% end %>
        <ul class="right">
          <% if user_signed_in? %>
            <li><%= link_to t('devise.actions.sign_out'), destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to t('devise.actions.sign_in'), new_user_session_path %></li>
          <% end %>
      </section>
    </nav>
    <div class="main-container">
      <div class="left-column">
        &nbsp;
      </div>        
      <div class="right-column">
        <% if notice %>
          <p class="notice"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="alert"><%= alert %></p>        
        <% end %>
        <%= yield %>
      </div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>